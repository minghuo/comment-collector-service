评论采集中间服务项目需求文档

版本	日期	作者	变更说明
v1.0	2026-05-15	架构组	初稿
1. 项目概述
1.1 项目背景
当前业务需要从多个社交平台（微博、微信、抖音、小红书等）批量采集评论数据，以及后续扩展至互动指标更新、账号历史等服务。每个平台可能存在多个数据供应商（官方API、第三方服务、自研爬虫），各供应商在成本、速率、稳定性上存在差异。需要一套统一的中间服务，实现供应商的灵活调度、优先级路由、故障切换及资源公平分配。

1.2 项目目标
构建高可用、可扩展的采集中间服务，屏蔽底层多供应商差异。

支持动态配置多平台、多供应商的优先级、频次、健康状态等。

实现基于任务量和用户优先级的智能路由，防止资源饥饿。

提供即时返回与异步任务两种模式，支持断点续采。

支持未来新增业务类型（如互动指标、账号历史等）而不必改动核心路由逻辑。

1.3 范围
本期包含：微博评论采集功能，集成三个供应商（本地爬虫、A公司接口、微博官方接口）。

框架设计预留微信、抖音、小红书等多平台扩展点。

服务类型暂仅评论采集，但架构需支持互动指标更新、账号历史等。

不包括具体爬虫实现，仅负责任务调度、路由、执行管理、结果持久化。

2. 术语定义
术语	说明
平台	社交媒体平台，如微博、微信、抖音、小红书
功能/服务类型	具体业务，如：评论采集、互动指标更新、账号历史查询
供应商	实现某一平台某一功能的数据提供方，如微博评论的本地爬虫
主任务	由用户提交的一个完整采集请求，关联一个或多个采集对象（链接）
子任务	将主任务按链接、供应商维度拆分的执行单元，由调度器分配给具体供应商执行
即时返回	同步等待采集结果并直接返回给用户
任务模式	接受请求后返回任务ID，异步执行，用户通过查询接口获取结果
阈值	触发启用低优先级供应商的任务队列长度或数量设定
用户优先级	标识用户重要程度的等级，影响资源分配和起始供应商选择
3. 功能性需求
3.1 多平台与多供应商管理
FR-01：系统支持注册多个平台，每个平台可配置多种功能。

FR-02：每个功能下可管理多个供应商，配置项包括：

供应商唯一标识

请求频次上限（如1秒2次）

最大重试次数

健康状态（自动探活 + 人工标记）

优先级（数字越小优先级越高）

是否支持即时返回

单次最大处理链接数（用于任务拆分）

FR-03：供应商配置支持运行时动态加载与更新（可通过数据库配置热刷新）。

FR-04：提供健康检查机制，定期探测供应商可用性，连续失败达到阈值自动降级，恢复后自动上线。

3.2 优先级与阈值路由
FR-05：当某个功能接收到任务时，根据平台+功能类型路由到对应供应商列表。

FR-06：默认按供应商优先级顺序分配，优先级高的供应商先被使用。

微博评论优先级示例：本地爬虫(1) > A公司接口(2) > 微博官方(3)。

FR-07：支持按任务队列深度设置阈值：

当高优供应商的待处理子任务数（或预估等待时间）超过阈值时，自动启用次优先级供应商分流。

FR-08：支持特定指标需求时，用户可以指定必须使用的供应商或供应商类别（如需要“高精度”数据，必须用官方接口），绕过全局优先级。

FR-09：当某供应商不可用时，自动将新任务分配给次优先级可用供应商，正在执行的任务失败后按重试策略处理。

3.3 用户优先级与资源分配
FR-10：系统维护用户等级（如1-5级），1级最高。

FR-11：多用户同时调用同一功能时，系统基于用户优先级和提交时间综合排序分配子任务执行机会。

FR-12：高优先级用户可优先使用高优供应商，低优先级用户起始供应商列表可限制在较低优先级区间（如高优用户可用全部供应商，低优用户仅能用A公司和微博官方）。

FR-13：防止高优任务阻塞低优任务：系统监控低优任务等待时间，若超过阈值（如30分钟），临时提升其优先级至较高等级，确保获得执行机会。

3.4 任务提交与路由
FR-14：用户通过 RESTful API 提交主任务，包含：

平台类型、功能类型、采集链接列表（支持批量）

返回模式：即时返回 / 任务模式

可选：指定供应商约束

FR-15：系统对入参进行校验（平台存在、功能存在、链接格式等），校验失败立即返回错误。

FR-16：参数校验通过后，根据业务规则生成主任务记录，并根据链接数量和供应商能力拆分为多个子任务。

FR-17：子任务进入基于 Redis 或 MongoDB 的有序队列（按用户优先级+时间排序），由调度器分配给供应商执行器。

3.5 采集模式
FR-18：即时返回模式：

仅当所有选定供应商都支持即时返回且链接数较少（可配置）时允许。

同步等待所有子任务完成，聚合结果后一次性返回给调用方。

支持超时控制，超时后降级为任务模式。

FR-19：任务模式：

提交后立即返回主任务ID。

用户通过任务查询接口获知完成状态及结果。

适用于批量链接、不支持即时返回的供应商或长耗时采集。

3.6 断点续采与重启恢复
FR-20：项目重启时，能自动恢复未完成的主任务及其下所有未完成子任务，继续调度执行。

FR-21：对于正在执行中的子任务，在重启时标记为“中断”，根据重试策略重新入队执行，保证不重复采集过多数据（依赖供应商的幂等性或采集参数去重）。

FR-22：支持任务记录持久化，包括已采集的页数、cursor等，使续采能从断点继续（由子任务执行器逻辑配合）。

3.7 多业务扩展
FR-23：系统设计为通用任务调度框架，评论采集作为第一种功能实例。

FR-24：新增“互动指标更新”、“账号历史”等功能时，只需在数据库配置新的功能类型和供应商，无需修改调度核心代码。

FR-25：每个功能可定义自己的参数Schema，由主任务提交时携带，校验使用动态Schema校验。

4. 非功能性需求
4.1 性能要求
支持并发提交任务数：≥500 TPS。

子任务调度延迟：P99 < 1秒（除供应商自身延迟外）。

供应商频次控制精准度误差：≤5%。

4.2 可用性
服务自身可用性 ≥ 99.9%。

供应商故障切换响应时间 < 30秒。

单点故障（如数据库、Redis）可通过主从或集群规避。

4.3 可扩展性
供应商、平台、功能可动态增减，无需停服。

调度器支持水平扩展，通过分布式锁或分片队列实现。

4.4 可维护性
配置热加载，提供管理后台接口。

完善的监控（任务积压、供应商QPS、错误率）与日志。

接口文档符合OpenAPI 3.0规范。

5. 架构设计概要
5.1 系统架构图（文字描述）
┌─────────┐     ┌────────────────────────────────────────────┐
│ 用户端  │────▶│   API 网关 / 负载均衡                      │
└─────────┘     └────────────────────────────────────────────┘
                          │
         ┌────────────────┴────────────────┐
         ▼                                  ▼
┌──────────────────┐              ┌──────────────────┐
│  任务管理服务     │              │  配置管理服务     │
│ - 主任务CRUD     │              │ - 平台/功能/     │
│ - 参数校验       │              │   供应商配置      │
│ - 任务拆分       │              │ - 用户优先级      │
│ - 结果聚合       │              │ - 热刷新         │
└────────┬─────────┘              └────────┬─────────┘
         │                                  │
         └──────────┬───────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│              路由与调度引擎                  │
│  - 基于优先级的队列管理 (Redis Sorted Set)  │
│  - 阈值监测与供应商选择                     │
│  - 频次控制 (令牌桶/滑窗)                   │
│  - 分布式锁防重复                            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────┐
│           供应商执行器集群 (Worker)           │
│  - 每个供应商对应一个执行器适配器             │
│  - 拉取子任务，执行HTTP/爬虫调用              │
│  - 结果回写及重试逻辑                        │
└──────────────────┬───────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────┐
│           MongoDB / Redis                     │
│  - 任务数据、配置、队列、状态缓存             │
└──────────────────────────────────────────────┘
5.2 技术选型
开发框架：Spring Boot 3.2.5 (Java 17+)

数据库：MongoDB 6.0+（主任务、子任务、配置、结果存储）

缓存与队列：Redis 7.0+（优先级队列、频次控制、分布式锁）

通信协议：RESTful API（对外），内部执行器通过 Redis 队列或轻量MQ解耦

监控：Micrometer + Prometheus + Grafana

5.3 核心模块
task-api：对外暴露的任务提交、查询接口。

task-core：主任务管理、参数校验、子任务拆分。

supplier-router：实现优先级、阈值、用户优先级路由逻辑。

supplier-executor：各供应商适配器，封装调用细节，受频次控制器控制。

config-manager：平台/功能/供应商/用户配置CRUD及缓存。

health-checker：探活、故障摘除与恢复。

recovery：重启后任务重加载与重调度。

6. 数据库设计概要
6.1 MongoDB 集合设计
集合名	说明	关键字段
platform	平台定义	_id, name, status
function	功能定义（关联平台）	_id, platformId, name, paramSchema
supplier	供应商定义	_id, functionId, name, priority, maxQps, maxRetry, healthStatus, supportsSync, maxBatchLinks
user_priority	用户优先级配置	userId, level, allowedSupplierMinPriority, boostThresholdSec
master_task	主任务	_id, userId, functionId, requestParams, links[], mode(SYNC/ASYNC), status, createTime, updateTime
sub_task	子任务	_id, masterTaskId, supplierId, link, status, priority(derived), retryCount, nextExecuteTime, cursor, result, createTime, updateTime
supplier_state	供应商运行时状态	supplierId, currentQps, queueDepth, lastHeartbeat, consecutiveFailures
6.2 关键设计思想
子任务的priority字段根据用户优先级动态计算，用于 Redis Sorted Set 排序。

supplier_state通过定时任务更新，用于路由决策。

重启恢复：服务启动时扫描master_task和sub_task中状态为PROCESSING或PENDING的记录重新入队。

7. 接口设计
7.1 任务提交接口
POST /api/v1/tasks
Content-Type: application/json
{
  "platform": "weibo",
  "function": "comment_collect",
  "links": ["https://weibo.com/...", ...],
  "mode": "ASYNC",                  // SYNC / ASYNC
  "supplierConstraint": {          // 可选
    "mustUse": ["local_crawler"],  // 指定供应商ID
    "preferredPriorityMin": 2
  },
  "callbackUrl": "https://..."     // 可选，任务完成回调
}
Response:
{
  "code": 0,
  "data": {
    "masterTaskId": "MT20260515...",
    "mode": "ASYNC",
    "status": "ACCEPTED"
  }
}
7.2 任务查询接口
GET /api/v1/tasks/{masterTaskId}
Response:
{
  "code": 0,
  "data": {
    "masterTaskId": "...",
    "status": "COMPLETED",        // PENDING / PROCESSING / PARTIAL_SUCCESS / COMPLETED / FAILED
    "links": [
      {
        "link": "https://...",
        "status": "SUCCESS",
        "supplier": "A_company",
        "result": { ... },
        "subTaskId": "ST..."
      }
    ],
    "createTime": "2026-05-15T10:00:00Z",
    "updateTime": "2026-05-15T10:05:00Z"
  }
}
7.3 管理接口（举例）
供应商配置 CRUD：/api/v1/admin/suppliers

用户优先级配置：/api/v1/admin/user-priorities

健康状态手动覆盖：/api/v1/admin/suppliers/{id}/health

所有管理接口需鉴权。
8. 关键流程
8.1 任务提交与路由流程
用户提交请求，task-api校验平台、功能、链接格式。

task-core创建主任务记录，status=PENDING，根据mode设置。

根据functionId加载供应商列表（按priority升序）。

应用用户优先级约束，过滤供应商起始列表。

若用户指定supplierConstraint，覆盖默认列表。

遍历links，为每个link创建子任务，初始供应商为路由决策选定供应商（根据队列深度、可用性）。

子任务入队（Redis Sorted Set，score=用户优先级*时间因子），存储至MongoDB。

若mode=SYNC且支持，进入同步等待聚合；否则立刻返回masterTaskId。

调度器拉取子任务分配给对应供应商executor，受频次控制。

executor执行，成功则更新子任务状态，聚合结果；失败则重试或fallback至低优供应商。

8.2 阈值与优先级动态调整流程
每个供应商维护一个本地队列或Redis队列长度指标。

调度器在选择供应商时，检查最高优供应商的待处理子任务数（或预测等待时间）是否超过阈值（threshold_pending）。

超过则激活次优供应商参与分流，新子任务按比例分配给次优。

低优先级用户等待超时提升：定时任务扫描sub_task中status=PENDING且waiting_time>30分钟的用户，将用户标识加入“临时高优池”，其子任务重新计算score。

8.3 健康检查与故障切换
health-checker定期（15s）调用供应商探活接口。

连续失败N次（配置）后，更新supplier_state为DOWN。

router将DOWN供应商从可用列表移除，新任务不再分配。

恢复后自动上线。

正在执行的任务若失败，按重试策略（同供应商重试maxRetry次，仍失败则选择fallback供应商重试）。

8.4 重启恢复流程
应用启动完成后执行TaskRecoveryRunner。

查询所有status为PROCESSING或PENDING的主任务和子任务。

对于PROCESSING子任务，状态更新为PENDING，重置retryCount。

所有PENDING子任务重新计算score入队Redis。

确保不会重复创建主任务。

9. 项目计划与里程碑（建议）
阶段	内容	产出	周期
P0	核心框架搭建，微博评论单平台三供应商实现	可用MVP，支持配置路由、任务模式	2周
P1	用户优先级、阈值分流、即时返回、恢复重启	完整调度能力	1.5周
P2	多平台扩展（微信、抖音、小红书），通用业务模型	平台功能配置化	1.5周
P3	监控、管理后台、性能压测与优化	生产就绪	1周
10. 风险评估
风险	影响	缓解措施
供应商不稳定导致大量重试	任务积压、系统负载高	快速熔断，fallback策略，合理阈值设置
MongoDB单点性能瓶颈	任务读写慢	分片集群，读写分离，关键路径使用Redis
用户优先级频繁提升导致资源争抢	高低优用户感受趋同	严格控制提升比例和频次，监控
供应商频次控制不准	触发限流被禁	采用滑窗计数器结合令牌桶，集群下使用Redis全局计数
多服务扩展参数校验复杂	开发成本高	定义Schema模板，使用JSON Schema校验动态参数